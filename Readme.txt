
 run the project successfully, you should have Python installed.

1. Go to you project directory, Open StreamlitCode.py in your code editor and replace the PATH variable on line 36 with the path of restnet18_net.pth in your system. Then, save the code.

2. Now go to your project directory, right click on empty space in your window, right click and click on 'Open in Terminal', this will open Command Prompt in your project directory.

IN CMD :

3. Copy and Paste this : pip install streamlit streamlit_option_menu opencv-python torch torchvision numpy pillow validators


This will install all the necessary libraries.


4. Now in the same cmd, run this line : streamlit run StreamlitCode.py